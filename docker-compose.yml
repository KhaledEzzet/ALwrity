services:
  alwrity-backend:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    restart: unless-stopped
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      HF_TOKEN: ${HF_TOKEN:-}
      GPT_PROVIDER: ${GPT_PROVIDER:-gemini}
      DATABASE_URL: ${DATABASE_URL:-sqlite:///./data/alwrity.db}
      SECRET_KEY: ${SECRET_KEY}
      HF_HOME: /app/backend/data/huggingface
      TRANSFORMERS_CACHE: /app/backend/data/huggingface
    volumes:
      - alwrity_backend_data:/app/backend/data
    expose:
      - "8000"

  alwrity-frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
      args:
        REACT_APP_API_URL: ${REACT_APP_API_URL}
    restart: unless-stopped
    depends_on:
      - alwrity-backend
    expose:
      - "3000"

volumes:
  alwrity_backend_data: